export type ProtoUser = {
    name?: string;
    email?: string;
    password?: string
    Role?: string
}

export type User = {
    id: string;
    name: string;
    email: string;
    password: string
    Role: string
}
